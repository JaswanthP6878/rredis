# TODO
- [x] Rerwite the parser to be more performant (and reads frames)
- [ ] Make changes the socket handling part to read data properly, instead of reading line [main.rs](src/main.rs#L55-L63)
    - [ ] split the logic into a handler and listener part(need to understand signal handling for clean break in connections)

